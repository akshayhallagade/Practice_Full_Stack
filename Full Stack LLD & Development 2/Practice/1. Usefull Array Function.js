// // ..................1. ForEach Function .....................
// let total = 0;
// [1, 2, 3, 4, 4, 5, 6, 7].forEach((item) => (total = total + item));
// total;

// // ..................2. Map Function .........................
// const answer = [1, 2, 3, 4, 45, 5].map((item) => item + 2);
// answer;

// // ..................3. Filter Function .....................
// const answer = [1,2,3,4,5,6].filter(item=> item %2==0)
// answer;

// // ..................4. Reduce Function .....................
// const answer = [1, 2, 3, 4, 4, 6, 6].reduce((total, item) => {
//   return total + item;
// }, 0);
// answer;
